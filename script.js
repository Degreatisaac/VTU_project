const service = document.querySelector('.services');
const toggleServe = document.querySelector('.serve');

service.addEventListener('click', function () {
    toggleServe.classList.toggle('add');
});

